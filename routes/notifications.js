// notifications.js

function sendAppointmentReminder(appointment) {
    console.log(`Enviando lembrete de agendamento para ${appointment.patient} em ${appointment.time}`);
    // Aqui você pode integrar com um serviço de e-mail ou SMS
}
